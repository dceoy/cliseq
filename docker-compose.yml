---
version: '3'

services:
  wgs-pipeline:
    build:
      context: .
      dockerfile: Dockerfile
    image: dceoy/cliseq:latest
    user: ${UID}:${GID}
    userns_mode: host
    volumes:
      - .:/wd
    working_dir: /wd
